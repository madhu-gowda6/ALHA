"""Tool: request_image — prompt farmer to upload an image via pre-signed S3 URL."""


async def request_image(session_id: str, prompt_text: str, prompt_text_hi: str) -> dict:
    """Request farmer to upload an image; return pre-signed upload URL."""
    pass
